# MineAdmin 前端生产模式 Dockerfile
# 基于 nginx Alpine 镜像运行构建后的静态文件

FROM nginx:alpine

# 设置环境变量
ENV TZ=Asia/Shanghai

# 复制 nginx 配置
COPY docker/nginx/default.conf /etc/nginx/conf.d/default.conf

# 创建必要的目录
RUN mkdir -p /var/log/nginx

# 暴露端口
EXPOSE 80

# 启动 nginx
CMD ["nginx", "-g", "daemon off;"]
